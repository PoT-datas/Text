<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pot.text">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/b_pot_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/w_pot_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".test.XActivity"></activity>
        <activity android:name=".test.ScrollActivity" />
        <activity android:name=".test.SmartActivity" />
        <activity android:name=".test.MagicActivity" />
        <activity android:name=".test.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>